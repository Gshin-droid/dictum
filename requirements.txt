# Нужно только для запуска из исходников и сборки exe.
# Готовому voice-dictation.exe ничего из этого не требуется.

onnx-asr[cpu,hub]>=0.12   # запуск моделей распознавания речи + скачивание весов
sounddevice>=0.5          # запись с микрофона (PortAudio)
numpy>=2.0
keyboard>=0.13            # глобальная горячая клавиша
pyperclip>=1.9            # текст в буфер обмена перед вставкой
pystray>=0.19             # значок в системном лотке
pillow>=11.0              # рисование значка
python-dotenv>=1.0        # чтение .env

# только для разработки
pytest>=8.0
pyinstaller>=6.20
